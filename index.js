/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import ScreenNavigation from './config/ScreenNavigation';

AppRegistry.registerComponent(appName, () => ScreenNavigation);
